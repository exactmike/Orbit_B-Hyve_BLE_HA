# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual envs
venv/
env/
.venv/
ENV/

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Local config / secrets — never commit these
config.local.json
config.json
*.local.yaml
.env
secrets.yaml
/scripts/.bhyve_config.json

# Local-only working brief (imports the real brief from the sibling
# Local_Orbit_B-Hyve_BLE_HA directory, which lives outside the repo).
/CLAUDE.md

# Local MCP server config (machine-specific).
/.mcp.json

# Captures & logs (may contain device-specific data)
*.btsnoop
*.pcap
*.pcapng
captures/
logs/

# Build artifacts
*.zip
release_build/
